#image:
#  file: .gitpod.Dockerfile


# List the start up tasks. Learn more https://www.gitpod.io/docs/config-start-tasks/
tasks:
  - init: sudo apt-get update && apt-get install -y biber latexmk make texlive-full curl && rm -rf /var/lib/apt/lists/*
    command: echo 'Echo from start script'

# List the ports to expose. Learn more https://www.gitpod.io/docs/config-ports/
ports:
  - port: 3000
    onOpen: open-preview
